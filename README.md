# Legacy source preservation

Archive-only maintenance branch. Not product integration; never merge into product branches. This workflow reads only the existing public ATLAS repository. It does not access or publish any private AXIS repository, code, credentials, documents or provider data. Original branch histories and bytes are preserved in a full verified Git bundle.
