# Google Connectors
Tokens are stored under `~/atlas_data/secrets/` and loaded via `atlas.config.yaml` path settings.
All API calls are passed as injectable function hooks so tests can mock them.
Use `scripts/atlas_import_gmail.sh` for Termux command hook.
