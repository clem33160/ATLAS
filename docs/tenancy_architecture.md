# Tenancy Architecture
Atlas SaaS Scale Foundation v1 introduces tenant model, isolation modes, tenant context, registry, and isolation policy.
Policy: local_single_tenant (Termux MVP), shared_db_tenant_id (early SaaS), schema_per_tenant (serious B2B), database_per_tenant or dedicated_stack (regulated enterprise).
